interface:
  display_name: "Push Reviewed Diff"
  short_description: "Push reviewed changes to origin after approval"
  default_prompt: "Use $push-reviewed-diff to push the current diff to origin as soon as I approve it."
